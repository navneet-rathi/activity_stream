- name: Get current workflow job ID
  hosts: localhost
  gather_facts: false
  tasks:
    - set_fact:
        job_id: "{{ lookup('env', 'awx_workflow_job_id') | int }}"
  
    - name: Show the workflow job ID
      debug:
        msg: "Current Workflow Job ID is {{ job_id }}"
